# Pod by Ricardo Erazo
apiVersion: v1
kind: Pod
metadata:
  name: borde-interno
  namespace: reggaeton
spec:
  containers:
    - name: wisin-yandel
      image: nginx
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: style
                operator: In
                values:
                  - urban